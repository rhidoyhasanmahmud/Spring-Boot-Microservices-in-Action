server:
  port: 9296

spring:
  profiles:
    active: native
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rhidoyhasanmahmud/config-server
          clone-on-start: true

